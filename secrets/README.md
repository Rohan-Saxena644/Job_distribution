# Secret Files

Run `./scripts/generate-local-secrets.ps1` from PowerShell before starting Docker Compose.

The generated files are ignored by Git. They are development credentials and a self-signed certificate, so replace them with secrets and certificates supplied by your deployment platform in production.
